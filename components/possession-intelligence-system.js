(()=>{'use strict';
const VERSION='0.62.2';
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const other=t=>t==='blue'?'red':'blue';
const now=()=>performance.now();
function boot(){
  const e=window.FutLiveFootballEngine;
  if(!e||!e.players?.length||typeof e.ownedAI!=='function'){setTimeout(boot,45);return}
  if(e.__possessionIntelligenceV0622)return;e.__possessionIntelligenceV0622=true;
  const oldOwned=e.ownedAI.bind(e),attack=t=>t==='blue'?1:-1;
  const living=p=>p&&!p.sentOff&&!p.tempSuspended;
  const fielders=t=>e.players.filter(p=>living(p)&&!p.goalkeeper&&(!t||p.team===t));
  const opponents=t=>fielders(other(t));
  const mates=(t,c)=>fielders(t).filter(p=>p!==c);
  const goalX=(t,f)=>t==='blue'?f.right:f.left;
  function nearestOppDist(team,q){const os=opponents(team);return os.length?Math.min(...os.map(o=>Math.hypot(q.x-o.x,q.y-o.y))):999}
  function pressureCount(p,r=54){return opponents(p.team).filter(o=>e.dist(p,o)<r).length}
  function laneSafety(c,t){const os=opponents(c.team),dx=t.x-c.x,dy=t.y-c.y,len2=dx*dx+dy*dy||1;let min=999,count=0;for(const o of os){const u=clamp(((o.x-c.x)*dx+(o.y-c.y)*dy)/len2,0,1),px=c.x+dx*u,py=c.y+dy*u,d=Math.hypot(o.x-px,o.y-py);min=Math.min(min,d);if(d<28)count++}return{min,count}}
  function marking(t){const n=opponents(t.team).filter(o=>e.dist(t,o)<52).length,close=opponents(t.team).filter(o=>e.dist(t,o)<34).length;return{n,close,space:nearestOppDist(t.team,t)}}
  function passScore(c,t,f){const a=attack(c.team),forward=(t.x-c.x)*a,dist=e.dist(c,t),mark=marking(t),lane=laneSafety(c,t),goalGain=Math.abs(goalX(c.team,f)-c.x)-Math.abs(goalX(c.team,f)-t.x);let score=forward*.28+goalGain*.18+mark.space*.62+lane.min*.68-dist*.06;score-=mark.n*24+mark.close*28+lane.count*24;if(t.personality==='finisher'&&forward>25)score+=14;if(t.personality==='wing'&&mark.space>48)score+=10;if(forward<0)score-=18;if(dist<34)score-=35;if(dist>f.w*.46)score-=42;if(mark.n>=3)score-=85;if(lane.count>=2)score-=55;return{score,mark,lane,forward,dist}}
  function bestPass(c,f){let best=null,bestMeta=null;for(const t of mates(c.team,c)){const m=passScore(c,t,f);if(!bestMeta||m.score>bestMeta.score){best=t;bestMeta=m}}return{target:best,meta:bestMeta}}
  function openGoalLane(c,f){const gx=goalX(c.team,f),gy=(f.goalTop+f.goalBottom)/2,dx=gx-c.x,dy=gy-c.y,len2=dx*dx+dy*dy||1;let min=999,count=0;for(const o of opponents(c.team)){const u=clamp(((o.x-c.x)*dx+(o.y-c.y)*dy)/len2,0,1),px=c.x+dx*u,py=c.y+dy*u,d=Math.hypot(o.x-px,o.y-py);min=Math.min(min,d);if(d<26)count++}return{min,count}}
  function shouldLongShoot(c,f,pressure){const dist=Math.abs(goalX(c.team,f)-c.x),ratio=dist/f.w;if(ratio<.16||ratio>.43)return false;const lane=openGoalLane(c,f),central=1-clamp(Math.abs(c.y-f.h*.5)/(f.h*.45),0,1),skill=c.skill?.shoot||.6,comp=c.skill?.composure||.6;let chance=.025+skill*.10+comp*.055+central*.055+(lane.min>34?.05:0)-pressure*.045-lane.count*.045;if(c.personality==='finisher')chance+=.045;if(ratio>.34)chance*=.58;return Math.random()<clamp(chance,.015,.22)}
  function tacticalSlot(p,c,f,index){const a=attack(p.team),homeY=f.h*(p.home?.[1]??.5),homeX=f.w*(p.home?.[0]??.5),span=f.bottom-f.top;const upper=homeY<f.h*.5,side=upper?-1:1;const carrierProgress=p.team==='blue'?(c.x-f.left)/(f.right-f.left):(f.right-c.x)/(f.right-f.left);let depth=58,laneY=homeY;if(p.personality==='finisher'){depth=carrierProgress>.68?84:118;laneY=clamp(c.y+side*span*.10,f.top+44,f.bottom-54)}else if(p.personality==='wing'){depth=carrierProgress>.72?62:104;laneY=clamp(homeY+side*span*.055,f.top+38,f.bottom-48)}else if(p.personality==='support'){depth=44;laneY=clamp(c.y+side*span*.23,f.top+42,f.bottom-52)}else{depth=-52;laneY=clamp(homeY*.58+c.y*.42,f.top+42,f.bottom-52)}let desiredX=c.x+a*depth;const edgePad=58,edgeL=f.left+edgePad,edgeR=f.right-edgePad;if(desiredX<edgeL||desiredX>edgeR){const fallback=homeX+(c.x-f.w*.5)*.10;desiredX=desiredX<edgeL?Math.max(edgeL,fallback):Math.min(edgeR,fallback)}laneY+=((index%3)-1)*18;return{x:clamp(desiredX,edgeL,edgeR),y:clamp(laneY,f.top+40,f.bottom-50)}}
  function repelTarget(p,q,team){let x=q.x,y=q.y;for(const m of fielders(team)){if(m===p)continue;const dx=x-m.x,dy=y-m.y,d=Math.hypot(dx,dy)||1;if(d<58){const push=(58-d)*.72;x+=dx/d*push;y+=dy/d*push}}return{x,y}}
  function supportRuns(c,dt,f){const list=mates(c.team,c).sort((a,b)=>(a.slot??0)-(b.slot??0));list.forEach((p,i)=>{let q=tacticalSlot(p,c,f,i);q=repelTarget(p,q,c.team);const minCarrier=p.personality==='creator'?46:62,dCarrier=Math.hypot(q.x-c.x,q.y-c.y);if(dCarrier<minCarrier){const dx=q.x-c.x,dy=q.y-c.y,d=dCarrier||1;q.x=c.x+dx/d*minCarrier;q.y=c.y+dy/d*minCarrier}q.x=clamp(q.x,f.left+48,f.right-48);q.y=clamp(q.y,f.top+40,f.bottom-50);const mul=(p.personality==='finisher'||p.personality==='wing') ? .82 : .66;e.moveToward(p,q.x,q.y,p.speed*mul,dt)})}
  function defendWhileOwned(c,dt,f){const os=opponents(c.team).sort((a,b)=>e.dist(a,c)-e.dist(b,c));os.forEach((p,i)=>{if(i===0){e.moveToward(p,c.x-attack(p.team)*14,c.y,p.speed*.84,dt);return}if(i===1){const side=p.home?.[1]<.5?-1:1;e.moveToward(p,c.x-attack(p.team)*64,c.y+side*42,p.speed*.66,dt);return}const hx=f.w*p.home[0],hy=f.h*p.home[1],shiftX=(c.x-f.w*.5)*.08,shiftY=(c.y-f.h*.5)*.12;e.moveToward(p,clamp(hx+shiftX,f.left+46,f.right-46),clamp(hy+shiftY,f.top+40,f.bottom-50),p.speed*.48,dt)})}
  function carrierMove(c,dt,f,pressure){const a=attack(c.team),gy=(f.goalTop+f.goalBottom)/2;let tx=c.x+a*(pressure>=2?24:pressure?34:48),ty=c.y+(gy-c.y)*(pressure?.08:.16);const os=opponents(c.team).filter(o=>e.dist(c,o)<72);if(os.length){let ax=0,ay=0;for(const o of os){const dx=c.x-o.x,dy=c.y-o.y,d=Math.hypot(dx,dy)||1,w=(74-Math.min(74,d))/74;ax+=dx/d*w;ay+=dy/d*w}tx+=ax*(pressure>=2?28:18);ty+=ay*(pressure>=2?34:22)}if(pressure>=3){tx=c.x-a*22;ty=c.y+(c.y<gy?-24:24)}e.moveToward(c,clamp(tx,f.left+34,f.right-34),clamp(ty,f.top+34,f.bottom-44),c.speed*(pressure>=3?.62:pressure>=2?.72:.88),dt);e.syncOwnedBall()}
  function decide(c,f,pressure){const t=now();if(t<(c.__possessionThinkAt||0))return false;const possess=t-(e.ownerSince||t),pick=bestPass(c,f),safe=pick.target&&pick.meta&&pick.meta.score>34&&pick.meta.mark.n<3&&pick.meta.lane.count<2,forwardSafe=safe&&pick.meta.forward>8;if(t>e.actionLock&&possess>520&&shouldLongShoot(c,f,pressure)){c.__possessionThinkAt=t+900;e.shoot(c,f);return true}if(pressure>=3){if(safe&&pick.meta.score>48){c.__possessionThinkAt=t+700;e.pass(c,pick.target);return true}c.__possessionThinkAt=t+380;return false}if(pressure>=2&&safe&&possess>620){c.__possessionThinkAt=t+720;e.pass(c,pick.target);return true}if(forwardSafe&&possess>900&&Math.random()<.46){c.__possessionThinkAt=t+760;e.pass(c,pick.target);return true}if(safe&&pick.meta.forward<0&&pressure>=1&&possess>1100){c.__possessionThinkAt=t+760;e.pass(c,pick.target);return true}c.__possessionThinkAt=t+(pressure?360:480);return false}
  e.ownedAI=(dt,f)=>{if(window.FutLiveMatchState?.phase&&window.FutLiveMatchState.phase!=='PLAYING')return;const c=e.ball.owner;if(!c)return;if(c.goalkeeper)return oldOwned(dt,f);if(c.sentOff||c.tempSuspended)return;const pressure=pressureCount(c,54);supportRuns(c,dt,f);defendWhileOwned(c,dt,f);carrierMove(c,dt,f,pressure);if(e.challengeOwner(c,dt))return;decide(c,f,pressure)};
  window.FutLivePossessionIntelligence={version:VERSION,bestPass,passScore,marking,laneSafety,pressureCount,shouldLongShoot,tacticalSlot};
}
boot();
})();